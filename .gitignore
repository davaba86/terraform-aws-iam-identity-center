# Terraform files
**/.terraform/*

# Terraform lockfile
.terraform.lock.hcl

# Terraform state files
*.tfstate
*.tfstate.*

# Exclude tfvars files, which may contain sensitive data, such as account IDs and ARNs, and so should not be checked in
*.tfvars

# Exclude local macOS files and IDE project files
.DS_Store
.idea
